# Which fallback tier is active
# Fallback Pilot - captured 2026-09-20 17:50
# Windows 11 | command: -m fallback_pilot.cli doctor
----------------------------------------------------------------------

┌───────────────────── Fallback Pilot - device readiness ─────────────────────┐
│   Network        up                                                         │
│   Local model    serving                                                    │
│   Endpoint       http://127.0.0.1:39839/v1                                  │
│   Cloud AI       withheld                                                   │
│   Active tier    1 - Local model (cloud AI withheld)                        │
└─────────────────────────────────────────────────────────────────────────────┘
  note: Network is up, but paid cloud AI is withheld by policy.
