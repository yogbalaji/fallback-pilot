# What this machine can do
# Fallback Pilot - captured 2026-09-13 19:28
# Windows 11 | command: scripts/check_device.py
----------------------------------------------------------------------

{
  "os": "Windows 11",
  "cpu": "Intel(R) Core(TM) Ultra 7 165H",
  "ram_gb": 31.6,
  "npu_present": [
    "Intel(R) AI Boost"
  ],
  "npu_family": "Meteor Lake (Core Ultra Series 1)",
  "npu_tops_approx": 11,
  "npu_usable_by_local_runtime": true,
  "copilot_plus_class": false,
  "gpus": [
    "Intel(R) Arc(TM) Graphics"
  ],
  "gpu_acceleration": "Intel - OpenVINO GPU execution provider",
  "phi_silica_supported": "no (Intel GPUs not supported)",
  "foundry_cli": "0.10.3"
}

================================================================
VERDICT: NPU PATH - a local runtime can target this NPU.
         Meteor Lake (Core Ultra Series 1), approx 11 TOPS.
         Below the 40 TOPS Copilot+ bar, so Windows'
         built-in on-device model APIs stay locked - but a portable
         runtime still uses the NPU. Confirm it: load a model, then
         watch Task Manager > Performance > NPU while generating.

  Phi Silica on this machine: no (Intel GPUs not supported)
  Note: Phi Silica is being replaced by Aion Instruct (Nov 2026).
        Prefer a portable local runtime over the built-in Windows APIs.

  31.6 GB RAM - comfortable. Try a 3B-4B model, not just 0.5B.
================================================================
